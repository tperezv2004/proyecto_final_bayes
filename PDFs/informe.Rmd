---
title: "Propuesta de Proyecto: Rendimiento en Powerlifting"
subtitle: "Metodos Bayesianos -- Primer Semestre 2026"
author: "Christian Camilo, Luna Garces, Tomas Perez, Tomas Romero, Benjamin Thareau"
output:
  pdf_document:
    toc: false
    fig_caption: true
    keep_tex: false
lang: es
header-includes:
  - \usepackage{float}
editor_options:
  markdown:
    wrap: 72
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  fig.align = "center",
  fig.pos = "H",
  out.width = "92%",
  fig.width = 7,
  fig.height = 4.2
)
set.seed(123)
```

```{r librerias, include=FALSE}
library(tidyverse)
library(lubridate)
library(knitr)
library(scales)
```

```{r carga-datos, include=FALSE}
ruta_datos <- "Data/reporte_USA.csv"
df_trabajo <- read_csv(ruta_datos, show_col_types = FALSE)

# Preparacion minima para asegurar que las variables usadas existan.
df_trabajo <- df_trabajo %>%
  mutate(Date = as.Date(Date))

if (!"Year" %in% names(df_trabajo)) {
  df_trabajo <- df_trabajo %>% mutate(Year = year(Date))
}

if (!"TipoEquipo" %in% names(df_trabajo)) {
  df_trabajo <- df_trabajo %>%
    mutate(
      TipoEquipo = case_when(
        Equipment == "Raw" ~ "Raw",
        Equipment %in% c("Wraps", "Single-ply", "Multi-ply") ~ "Equipado",
        TRUE ~ NA_character_
      )
    )
}

# Variable creada para una pregunta posterior del proyecto.
df_trabajo <- df_trabajo %>%
  mutate(
    Elite = if_else(Dots >= 400, "Elite", "No elite"),
    Sex = factor(Sex, levels = c("F", "M")),
    Equipment = factor(
      Equipment,
      levels = c("Raw", "Wraps", "Single-ply", "Multi-ply")
    ),
    TipoEquipo = factor(TipoEquipo, levels = c("Raw", "Equipado")),
    AgeClass = factor(
      AgeClass,
      levels = c(
        "13-15", "16-17", "18-19", "20-23", "24-34", "35-39",
        "40-44", "45-49", "50-54", "55-59", "60-64", "65-69", "70-74"
      )
    )
  )

n_registros <- nrow(df_trabajo)
n_variables <- ncol(df_trabajo)
n_atletas <- n_distinct(df_trabajo$Name)
anio_min <- min(df_trabajo$Year, na.rm = TRUE)
anio_max <- max(df_trabajo$Year, na.rm = TRUE)

colores_sexo <- c("F" = "#F8766D", "M" = "#00BFC4")
colores_equipo <- c(
  "Raw" = "#7CAE00",
  "Wraps" = "#C77CFF",
  "Single-ply" = "#00BFC4",
  "Multi-ply" = "#F8766D"
)
colores_tipo <- c("Raw" = "#7CAE00", "Equipado" = "#F8766D")

# Tema visual comun para mantener el informe limpio y consistente.
tema_informe <- theme_minimal(base_size = 11) +
  theme(
    plot.title = element_text(face = "bold", size = 13),
    plot.subtitle = element_text(size = 10),
    axis.title = element_text(size = 10),
    axis.text = element_text(size = 9),
    legend.title = element_text(face = "bold"),
    panel.grid.minor = element_blank(),
    strip.text = element_text(face = "bold"),
    plot.margin = margin(8, 8, 8, 8)
  )
```

# Introduccion

El powerlifting es un deporte de fuerza en el que los atletas compiten
principalmente en tres movimientos: sentadilla, press de banca y peso
muerto. A partir de estos levantamientos se obtiene el total levantado,
que corresponde a la suma del mejor intento en cada movimiento. Ademas,
existen puntajes ajustados, como DOTS y Wilks, que permiten comparar el
rendimiento entre atletas con distinto peso corporal.

Este proyecto busca estudiar el rendimiento de atletas de powerlifting
de Estados Unidos utilizando datos reales de OpenPowerlifting. Se busca
analizar como variables como sexo, edad, peso corporal, categoria de
peso y tipo de equipamiento se relacionan con el rendimiento deportivo.
El interes principal es entender que patrones se observan en los datos
y, posteriormente, utilizar modelos bayesianos para responder preguntas
de investigacion.

# Datos

## -.Procedencia

Los datos provienen de OpenPowerlifting, una base abierta que recopila
resultados de competencias de powerlifting a nivel internacional. La
base original contiene 3.920.662 registros y 42 variables, incluyendo
informacion del atleta, la competencia, los intentos de levantamiento,
los mejores levantamientos validos, puntajes ajustados y fecha del
evento. Para este proyecto se trabajo con una version filtrada de la
base, restringida a atletas de Estados Unidos (USA).

Es importante notar que cada fila representa un registro competitivo, no
necesariamente una persona unica. Por lo tanto, un mismo atleta puede
aparecer mas de una vez si compitio en distintas fechas o eventos. Esta
estructura es adecuada para analizar el rendimiento observado en
competencias, pero debe considerarse al interpretar resultados, porque
los registros no son completamente independientes a nivel de atleta.

## Variables involucradas

```{r tabla-variables}
tabla_variables <- tribble(
  ~Variable, ~Descripcion, ~Tipo, ~Unidad,
  "Name", "Nombre del atleta. Puede repetirse si el atleta compitio mas de una vez.", "Cualitativa", "No aplica",
  "Sex", "Sexo registrado en la competencia: F o M.", "Cualitativa", "No aplica",
  "Age", "Edad del atleta al momento de la competencia.", "Cuantitativa", "anhos",
  "AgeClass", "Clase de edad usada en la competencia.", "Cualitativa", "Rango de edad",
  "BodyweightKg", "Peso corporal registrado del atleta.", "Cuantitativa", "Kg",
  "WeightClassKg", "Categoria de peso en la que compite el atleta.", "Cualitativa", "Kg",
  "Equipment", "Tipo de equipamiento permitido: Raw, Wraps, Single-ply o Multi-ply.", "Cualitativa", "No aplica",
  "TipoEquipo", "Variable creada para agrupar Raw versus Equipado.", "Cualitativa", "No aplica",
  "Best3SquatKg", "Mejor intento valido de sentadilla dentro de los tres intentos principales.", "Cuantitativa", "Kg",
  "Best3BenchKg", "Mejor intento valido de press de banca dentro de los tres intentos principales.", "Cuantitativa", "Kg",
  "Best3DeadliftKg", "Mejor intento valido de peso muerto dentro de los tres intentos principales.", "Cuantitativa", "Kg",
  "TotalKg", "Suma de los mejores levantamientos validos en sentadilla, banca y peso muerto.", "Cuantitativa", "Kg",
  "Dots", "Puntaje ajustado por peso corporal usado para comparar rendimiento relativo.", "Cuantitativa", "Puntos",
  "Wilks", "Otro puntaje ajustado usado historicamente en powerlifting.", "Cuantitativa", "Puntos",
  "Date", "Fecha de inicio de la competencia.", "Fecha", "Fecha",
  "Year", "anho de la competencia, creado a partir de Date.", "Cuantitativa discreta", "anho",
  "Elite", "Variable creada para clasificar atletas con Dots >= 400 como Elite.*", "Cualitativa", "No aplica"
)

kable(tabla_variables, align = "llll")
```

\*El umbral de DOTS >= 400 para clasificar atletas como élite fue
definido operacionalmente para este estudio, basándose en la
distribución observada en los datos (percentil aproximado X de la
muestra) y en referencias informales de la comunidad de powerlifting
competitivo. No existe un criterio oficial universalmente aceptado

```{r tabla-equipamiento}
tabla_equipamiento <- tribble(
  ~Categoria, ~`Principal caracteristica`, ~`Ayuda al levantamiento?`,
  "Raw", "Minimo soporte", "Solo proteccion/estabilidad.",
  "Wraps", "Vendas elasticas en rodillas", "Asistencia mecanica en sentadilla.",
  "Single-ply", "1 capa de poliester", "Gran asistencia; altera la tecnica.",
  "Multi-ply", "Varias capas de lona/poliester", "Maxima asistencia artificial."
)

kable(tabla_equipamiento, align = "lll")
```

## Preparacion y limpieza de los datos

Para construir la base final se seleccionaron variables relacionadas con
la identificacion del atleta, caracteristicas fisicas, tipo de
competencia, equipamiento, intentos de levantamiento, mejores marcas,
puntajes de rendimiento y fecha. Luego se eliminaron observaciones con
valores faltantes en las variables centrales del analisis.

Ademas, se aplicaron filtros para trabajar con registros comparables:
atletas de USA, evento SBD, sexo F o M, edades entre 15 y 70 anhos, peso
corporal entre 35 y 200 kg, levantamientos positivos, puntaje DOTS mayor
a 10 y competencias desde el anho 2000 en adelante.

```{r tamanho-muestra}
tabla_muestra <- tibble(
  Indicador = c(
    "Registros finales",
    "Variables finales",
    "Atletas distintos",
    "Primer anho observado",
    "Ultimo anho observado"
  ),
  Valor = c(
    comma(n_registros),
    comma(n_variables),
    comma(n_atletas),
    anio_min,
    anio_max
  )
)

kable(tabla_muestra, align = "lr")
```

# Analisis descriptivo

## Medidas de resumen

```{r tabla-resumen}
vars_resumen <- c(
  "Age", "BodyweightKg", "Best3SquatKg", "Best3BenchKg",
  "Best3DeadliftKg", "TotalKg", "Dots", "Wilks"
)

tabla_resumen <- df_trabajo %>%
  summarise(
    across(
      all_of(vars_resumen),
      list(
        Media = ~mean(.x, na.rm = TRUE),
        Mediana = ~median(.x, na.rm = TRUE),
        DE = ~sd(.x, na.rm = TRUE),
        Minimo = ~min(.x, na.rm = TRUE),
        Maximo = ~max(.x, na.rm = TRUE)
      ),
      .names = "{.col}_{.fn}"
    )
  ) %>%
  pivot_longer(
    everything(),
    names_to = c("Variable", "Medida"),
    names_pattern = "(.*)_(.*)",
    values_to = "Valor"
  ) %>%
  pivot_wider(names_from = Medida, values_from = Valor) %>%
  mutate(
    Variable = recode(
      Variable,
      Age = "Edad",
      BodyweightKg = "Peso corporal",
      Best3SquatKg = "Mejor sentadilla",
      Best3BenchKg = "Mejor banca",
      Best3DeadliftKg = "Mejor peso muerto",
      TotalKg = "Total levantado",
      Dots = "DOTS",
      Wilks = "Wilks"
    ),
    across(where(is.numeric), ~round(.x, 2))
  )

kable(tabla_resumen, align = "lrrrrr")
```

La tabla muestra que la base contiene atletas con distintos niveles de
rendimiento, edades y pesos corporales. Esto es importante para el
proyecto, porque permite comparar grupos y estudiar como cambia el
rendimiento segun las caracteristicas fisicas y competitivas.

## Distribucion de variables principales

```{r histogramas, fig.cap="Distribucion de edad, peso corporal y puntaje DOTS", fig.height=5.1}
df_trabajo %>%
  select(
    "Edad" = Age,
    "Peso corporal (kg)" = BodyweightKg,
    "Puntaje DOTS" = Dots
  ) %>%
  pivot_longer(everything(), names_to = "Variable", values_to = "Valor") %>%
  ggplot(aes(x = Valor)) +
  geom_histogram(bins = 30, fill = "steelblue", color = "white", alpha = 0.85) +
  facet_wrap(~Variable, scales = "free", ncol = 1) +
  labs(
    title = "Distribucion de variables principales",
    x = NULL,
    y = "Frecuencia"
  ) +
  tema_informe
```

La mayor parte de los registros corresponde a atletas adultos jovenes
(aproximadamente entre 20 y 40 anhos). El peso corporal se concentra en
rangos intermedios y el puntaje DOTS muestra una distribucion unimodal,
con mayor densidad en valores medios y una cola derecha asociada a
atletas de rendimiento excepcional. Esto sugiere que la muestra contiene
una mayoria de competidores recreativos o intermedios, junto con un
grupo mas pequeno de atletas de alto nivel.

## Rendimiento segun sexo

```{r boxplot-sexo, fig.cap="Distribucion del puntaje DOTS segun sexo", fig.height=4}
ggplot(df_trabajo, aes(x = Sex, y = Dots, fill = Sex)) +
  geom_boxplot(alpha = 0.75, outlier.alpha = 0.30, width = 0.55) +
  scale_fill_manual(values = colores_sexo) +
  labs(
    title = "Distribucion del puntaje DOTS segun sexo",
    x = "Sexo",
    y = "Puntaje DOTS"
  ) +
  tema_informe +
  theme(legend.position = "none")
```

En la muestra, los hombres presentan una mediana de DOTS algo mayor que
las mujeres, el rendimiento tambien puede depender de la edad, el peso
corporal, la categoria de peso y el tipo de equipamiento.

```{r densidad-sexo, fig.cap="Densidad del puntaje DOTS segun sexo", fig.height=4}
ggplot(df_trabajo, aes(x = Dots, fill = Sex, color = Sex)) +
  geom_density(alpha = 0.35, linewidth = 0.7) +
  scale_fill_manual(values = colores_sexo) +
  scale_color_manual(values = colores_sexo) +
  labs(
    title = "Distribucion del puntaje DOTS por sexo",
    x = "Puntaje DOTS",
    y = "Densidad",
    fill = "Sexo",
    color = "Sexo"
  ) +
  tema_informe
```

El grafico de densidad confirma que las distribuciones no son identicas.
La curva de hombres aparece desplazada hacia valores mas altos, aunque
existe un solapamiento importante entre ambos grupos.

## Rendimiento segun tipo de equipamiento

```{r boxplot-equip, fig.cap="Puntaje DOTS segun tipo de equipamiento", fig.height=4.2}
ggplot(df_trabajo, aes(x = Equipment, y = Dots, fill = Equipment)) +
  geom_boxplot(alpha = 0.75, outlier.alpha = 0.28, width = 0.6) +
  scale_fill_manual(values = colores_equipo, drop = FALSE) +
  labs(
    title = "Puntaje DOTS segun tipo de equipamiento",
    x = "Equipamiento",
    y = "Puntaje DOTS",
    fill = "Equipamiento"
  ) +
  tema_informe
```

El puntaje DOTS cambia segun el tipo de equipamiento. Las categorias
equipadas, especialmente *Single-ply* y *Multi-ply*, tienden a presentar
mayor dispersion y valores altos.

```{r boxplot-deadlift, fig.cap="Peso muerto en clase 90 kg segun equipamiento", fig.height=4.2}
df_peso90 <- df_trabajo %>%
  filter(
    WeightClassKg == "90",
    Equipment %in% c("Raw", "Wraps", "Single-ply", "Multi-ply"),
    Best3DeadliftKg > 0
  )

ggplot(df_peso90, aes(x = Equipment, y = Best3DeadliftKg, fill = Equipment)) +
  geom_boxplot(alpha = 0.80, outlier.alpha = 0.40, width = 0.6) +
  scale_fill_manual(values = colores_equipo, drop = FALSE) +
  labs(
    title = "Peso muerto en clase 90 kg: Raw versus equipado",
    subtitle = "Se fija la clase de peso para hacer una comparacion mas justa.",
    x = "Equipamiento",
    y = "Mejor peso muerto (kg)",
    fill = "Equipamiento"
  ) +
  tema_informe
```

Al fijar la clase de peso en 90 kg se reduce parte de la variabilidad
asociada al peso corporal. Las diferencias entre equipamientos permiten
observar de forma mas directa si el uso de soporte se asocia con mejores
marcas de peso muerto.

## Relacion entre Wilks y total levantado

```{r wilks-total, fig.cap="Relacion entre puntaje Wilks y total levantado segun sexo", fig.height=4.4}
ggplot(df_trabajo, aes(x = Wilks, y = TotalKg, color = Sex)) +
  geom_point(alpha = 0.28, size = 1.1) +
  geom_smooth(method = "lm", se = FALSE, linewidth = 0.9) +
  scale_color_manual(values = colores_sexo) +
  labs(
    title = "A mayor puntaje Wilks, mayor total levantado",
    subtitle = "La relacion se observa en ambos sexos, pero con niveles distintos de total levantado.",
    x = "Puntos Wilks",
    y = "Total levantado (kg)",
    color = "Sexo"
  ) +
  tema_informe
```

Existe una relacion positiva clara entre Wilks y el total levantado.
Esto es esperable, porque Wilks resume parte del rendimiento ajustando
por peso corporal. Ademas, se observa separacion entre hombres y mujeres
en el total absoluto levantado.

## Promedio de levantamientos segun edad

```{r levantamientos-edad, fig.cap="Promedio de sentadilla, banca y peso muerto segun clase de edad", fig.height=4.5}
df_trabajo %>%
  filter(!is.na(AgeClass)) %>%
  group_by(AgeClass) %>%
  summarise(
    Sentadilla = mean(Best3SquatKg, na.rm = TRUE),
    Banca = mean(Best3BenchKg, na.rm = TRUE),
    `Peso muerto` = mean(Best3DeadliftKg, na.rm = TRUE),
    .groups = "drop"
  ) %>%
  pivot_longer(
    cols = c(Sentadilla, Banca, `Peso muerto`),
    names_to = "Levantamiento",
    values_to = "Promedio"
  ) %>%
  ggplot(aes(x = AgeClass, y = Promedio, color = Levantamiento, group = Levantamiento)) +
  geom_line(linewidth = 0.9) +
  geom_point(size = 1.8) +
  labs(
    title = "Promedio levantado segun clase de edad",
    x = "Clase de edad",
    y = "Peso levantado promedio (kg)",
    color = "Levantamiento"
  ) +
  tema_informe +
  theme(axis.text.x = element_text(angle = 90, hjust = 1, vjust = 0.5))
```

El promedio levantado aumenta desde las edades mas jovenes hasta los 30
y luego tiende a disminuir en edades mayores. El peso muerto y la
sentadilla suelen presentar promedios mas altos que la banca. Esto
sugiere que la edad no tiene un efecto lineal simple sobre el
rendimiento.

## Relacion entre edad y DOTS

```{r dispersion-dots, fig.cap="Relacion entre edad y puntaje DOTS segun sexo", fig.height=4.2}
ggplot(df_trabajo, aes(x = Age, y = Dots, color = Sex)) +
  geom_point(alpha = 0.16, size = 1.0) +
  geom_smooth(method = "loess", se = FALSE, linewidth = 1) +
  facet_wrap(~Sex) +
  scale_color_manual(values = colores_sexo) +
  labs(
    title = "Relacion entre edad y rendimiento",
    subtitle = "El puntaje DOTS tiende a subir en edades tempranas y luego estabilizarse o disminuir.",
    x = "Edad",
    y = "Puntaje DOTS",
    color = "Sexo"
  ) +
  tema_informe
```

La relacion entre edad y DOTS no parece completamente lineal. El
rendimiento aumenta en las primeras etapas, alcanza niveles altos en
adultos jovenes y luego muestra una tendencia a disminuir en edades
mayores.

## Relacion entre peso corporal y DOTS

```{r dispersion-peso, fig.cap="Relacion entre peso corporal y puntaje DOTS segun sexo", fig.height=4.2}
ggplot(df_trabajo, aes(x = BodyweightKg, y = Dots, color = Sex)) +
  geom_point(alpha = 0.16, size = 1.0) +
  geom_smooth(method = "loess", se = FALSE, linewidth = 1) +
  facet_wrap(~Sex, scales = "free_x") +
  scale_color_manual(values = colores_sexo) +
  labs(
    title = "Relacion entre peso corporal y rendimiento",
    x = "Peso corporal (kg)",
    y = "Puntaje DOTS",
    color = "Sexo"
  ) +
  tema_informe
```

Se evidencia que, a diferencia de los hombres, un mayor peso corporal en
las mujeres no se traduce automáticamente en levantar más peso. En el
segmento masculino, sí existe una relación directa entre el aumento de
peso y la fuerza aplicada.

## DOTS por sexo y clase de edad

```{r dots-edad-sexo, fig.cap="Puntaje DOTS por sexo y clase de edad", fig.height=5.2}
df_trabajo %>%
  filter(Sex %in% c("F", "M"), !is.na(AgeClass)) %>%
  ggplot(aes(x = Sex, y = Dots, fill = Sex)) +
  geom_boxplot(alpha = 0.75, outlier.alpha = 0.22, width = 0.55) +
  facet_wrap(~AgeClass, scales = "free_y", ncol = 4) +
  scale_fill_manual(values = colores_sexo) +
  labs(
    title = "Puntaje DOTS por sexo y categoria de edad",
    x = "Sexo",
    y = "Puntaje DOTS",
    fill = "Sexo"
  ) +
  tema_informe +
  theme(legend.position = "bottom")
```

El grafico por clase de edad muestra que las diferencias entre sexos se
mantienen en varias categorias etarias, aunque la magnitud de la
diferencia cambia segun el grupo. Tambien se aprecia que algunas clases
de edad presentan mayor variabilidad, posiblemente por diferencias en
tamanho muestral o por la mezcla de atletas con trayectorias deportivas
distintas.

## DOTS promedio por rangos de edad y peso

```{r heatmap-dots, fig.cap="DOTS promedio por rangos de edad y peso corporal", fig.height=4.6}
df_trabajo %>%
  mutate(
    RangoEdad = cut(Age, breaks = seq(10, 80, by = 10), include.lowest = TRUE),
    RangoPeso = cut(BodyweightKg, breaks = seq(30, 210, by = 10), include.lowest = TRUE)
  ) %>%
  group_by(RangoEdad, RangoPeso) %>%
  summarise(
    DotsPromedio = mean(Dots, na.rm = TRUE),
    n = n(),
    .groups = "drop"
  ) %>%
  filter(n >= 5) %>%
  ggplot(aes(x = RangoEdad, y = RangoPeso, fill = DotsPromedio)) +
  geom_tile(color = "white") +
  scale_fill_gradient(low = "#F7FBFF", high = "#2B6CB0") +
  labs(
    title = "DOTS promedio por rangos de edad y peso",
    subtitle = "Se muestran combinaciones con al menos 5 registros.",
    x = "Rango de edad",
    y = "Rango de peso corporal (kg)",
    fill = "DOTS promedio"
  ) +
  tema_informe +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

El mapa de calor resume el rendimiento relativo promedio (medido en
DOTS) según rangos de edad y peso corporal. Aunque la métrica DOTS está
diseñada en teoría para equilibrar la ventaja del peso, el gráfico
revela que los puntajes más altos no se distribuyen uniformemente,
concentrándose fuertemente en el rango de 20 a 40 anhos y las categorías
más pesadas (sobre los 120 kg). El gráfico muestra la interacción entre
ambas variables: el declive del rendimiento asociado al envejecimiento
no ocurre con la misma intensidad ni al mismo ritmo en todas las
categorías de peso. Las celdas con menos registros fueron excluidas para
garantizar que estas tendencias representen comportamientos
poblacionales estables.

## Correlacion entre variables numericas

```{r matriz-correlacion, fig.cap="Matriz de correlacion entre variables numericas principales", fig.height=5}
vars_corr <- c(
  "BodyweightKg", "Age", "Best3SquatKg", "Best3BenchKg",
  "Best3DeadliftKg", "TotalKg", "Dots", "Wilks"
)

nombres_corr <- c(
  BodyweightKg = "Peso corporal",
  Age = "Edad",
  Best3SquatKg = "Sentadilla",
  Best3BenchKg = "Banca",
  Best3DeadliftKg = "Peso muerto",
  TotalKg = "Total",
  Dots = "DOTS",
  Wilks = "Wilks"
)

corr_df <- df_trabajo %>%
  select(all_of(vars_corr)) %>%
  drop_na()

matriz_corr <- cor(corr_df)
colnames(matriz_corr) <- nombres_corr[colnames(matriz_corr)]
rownames(matriz_corr) <- nombres_corr[rownames(matriz_corr)]

as.data.frame(as.table(matriz_corr)) %>%
  as_tibble() %>%
  rename(Variable1 = Var1, Variable2 = Var2, Correlacion = Freq) %>%
  ggplot(aes(x = Variable1, y = Variable2, fill = Correlacion)) +
  geom_tile(color = "white") +
  geom_text(aes(label = round(Correlacion, 2)), size = 3) +
  scale_fill_gradient2(
    limits = c(-1, 1),
    low = "#B2182B",
    mid = "white",
    high = "#2166AC"
  ) +
  labs(
    title = "Correlacion entre variables principales",
    x = NULL,
    y = NULL,
    fill = "Correlacion"
  ) +
  tema_informe +
  theme(axis.text.x = element_text(angle = 45, hjust = 1))
```

La matriz muestra correlaciones altas entre los tres levantamientos y el
total, lo que es esperable porque el total se construye directamente a
partir de ellos. DOTS y Wilks tambien presentan una correlacion muy
alta, ya que ambos son puntajes ajustados por peso corporal y buscan
medir rendimiento relativo. En cambio, la edad suele tener asociaciones
mas debiles con las variables de fuerza, lo que sugiere que su efecto
puede depender de otros factores.

## Evolucion de competidores en el tiempo

```{r evolucion-temporal, fig.cap="Evolucion de participantes por anho segun tipo de equipamiento", fig.height=4.2}
df_trabajo %>%
  count(Year, TipoEquipo) %>%
  filter(!is.na(TipoEquipo)) %>%
  ggplot(aes(x = Year, y = n, color = TipoEquipo)) +
  geom_line(linewidth = 1) +
  geom_point(size = 1.8) +
  scale_color_manual(values = colores_tipo) +
  scale_y_continuous(labels = comma) +
  labs(
    title = "Evolucion de competidores por tipo de equipo",
    subtitle = "La caida del ultimo anho debe interpretarse considerando que no esta completo el anho 2026",
    x = "anho",
    y = "Numero de registros",
    color = "Tipo de equipo"
  ) +
  tema_informe
```

La evolucion temporal muestra cambios importantes en la participacion y
en la composicion por tipo de equipamiento. En particular, los registros
*Raw* tienden a ganar protagonismo en los anhos recientes, mientras que
las categorias equipadas quedan relativamente más concentradas en
periodos o grupos especificos.

# Preguntas de investigacion

Las siguientes preguntas surgen del interes del equipo por comprender
distintas dimensiones del rendimiento en powerlifting competitivo,
considerando tanto factores fisicos como el rol del equipamiento y la
evolucion del deporte en el tiempo.

**Pregunta 1:** ¿Existe una diferencia significativa en la media de los
kilogramos levantados en peso muerto entre atletas *Raw* y aquellos con
equipamiento dentro de una misma clase de peso?

> *Motivacion:* Esta pregunta permite aislar parcialmente el efecto del
> equipamiento al comparar atletas dentro de una misma categoria de
> peso. El interes bayesiano seria estimar la diferencia de medias entre
> grupos y cuantificar la incertidumbre asociada a esa diferencia.

**Pregunta 2:** ¿Como ha evolucionado la cantidad total de competidores
a lo largo del tiempo dependiendo de si utilizan o no equipamiento de
soporte?

> *Motivacion:* La popularidad relativa de las categorias *Raw* y
> equipadas puede haber cambiado en el tiempo. Modelar esta evolucion
> permite estudiar tendencias temporales y comparar tasas de crecimiento
> entre tipos de competencia.

**Pregunta 3:** ¿Hay diferencias significativas en el rendimiento
(puntaje DOTS) entre hombres y mujeres en USA, controlando por edad y
peso corporal?

> *Motivacion:* La comparacion directa por sexo puede estar confundida
> por diferencias de edad, peso corporal y composicion de la muestra. Un
> modelo con estas variables fijas permitiria evaluar diferencias en
> rendimiento.

**Pregunta 4:** ¿Cuales caracteristicas del atleta actuan como factores
predictivos para alcanzar el nivel de elite en USA?

> *Motivacion:* Clasificar atletas como elite o no elite permite
> formular un problema predictivo. Variables como edad, sexo, peso
> corporal, equipamiento y levantamientos pueden aportar informacion
> sobre la probabilidad de alcanzar un nivel alto de rendimiento.

**Pregunta 5:** ¿Cual es la relacion entre la edad, el peso corporal y
el puntaje DOTS en atletas de powerlifting en USA?

> *Motivacion:* Los gráficos descriptivos muestran que la relación entre
> edad y rendimiento no es lineal: el puntaje DOTS aumenta en atletas
> jóvenes y tiende a disminuir en edades mayores. Se utilizara un
> enfoque bayesiano, el cual permite incorporar incertidumbre en la
> estimación de los coeficientes y es especialmente útil en zonas con
> menos datos, como las edades extremas.

# Discusión preliminar

A partir del analisis descriptivo realizado, es posible identificar
algunas tendencias generales que orientan las preguntas de investigacion
planteadas. Primero, el rendimiento medido mediante DOTS presenta
diferencias entre sexos. Para una comparación justa, se deben incorporar
diferentes parámetros de efecto para cada sexo, controlando además por
edad y peso corporal, en lugar de basarse únicamente en diferencias
visuales de mediana.

Segundo, el equipamiento parece estar relacionado con cambios en el
rendimiento y con patrones distintos de participacion. Las categorias
equipadas muestran mayor dispersion en algunos graficos, mientras que
los registros *Raw* parecen tener una presencia creciente en el tiempo.
Estas observaciones justifican preguntas especificas sobre el efecto del
equipamiento y sobre la evolucion temporal de las categorias.

Tercero, la edad y el peso corporal no se relacionan con el rendimiento
de forma puramente lineal. El rendimiento promedio tiende a aumentar
desde edades tempranas, estabilizarse en adultos jovenes y disminuir en
edades mayores. Ademas, el mapa de calor sugiere que ciertas
combinaciones de edad y peso concentran mejores puntajes promedio. Por
lo tanto, los modelos posteriores deberian considerar posibles efectos
no lineales e interacciones.

Finalmente, el analisis confirma que DOTS y Wilks capturan informacion
similar sobre rendimiento relativo, mientras que el total levantado y
los tres levantamientos principales estan fuertemente relacionados entre
si. Esto ayuda a decidir que variables usar como respuesta y cuales
utilizar como predictores, evitando redundancias innecesarias en los
modelos bayesianos.

# Referencias

-   OpenPowerlifting. (s.f.). *Open Powerlifting Database*. Recuperado
    de <https://www.openpowerlifting.org>
-   Lennon, E. (2024). *Raw vs. Equipped Powerlifting: Key Differences*.
    Lift Vault.
    <https://liftvault.com/resources/raw-vs-equipped-powerlifting/>
